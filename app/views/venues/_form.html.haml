= form_for(@venue) do |f|
  - if @venue.errors.any?
    #errorExplanation
      %h2= "#{pluralize(@venue.errors.count, 'error')} prohibited this user from being saved:"
      %ul
      - @venue.errors.full_messages.each do |msg|
        %li= msg
  

  .actions
    = f.submit
